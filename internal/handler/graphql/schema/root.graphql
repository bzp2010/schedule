scalar Int64

enum Status {
    ENABLED
    DISABLED
}

type Query {
    task(id: ID!): Task
    tasks(limit: Int! = 10, offset: Int! = 0): [Task!]!
    job(id: ID!): Job
    jobs(limit: Int! = 10, offset: Int! = 0, reverse_order: Boolean! = true): [Job!]!
}

interface Model {
    id: ID!
    created_at: Int64!
    updated_at: Int64!
}
